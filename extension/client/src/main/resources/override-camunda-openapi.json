{
  "components": {
    "schemas": {
      "LockedExternalTaskDto": {
        "properties": {
          "extensionProperties": {
            "type": "object",
            "nullable": true,
            "additionalProperties": {
              "type": "string"
            },
            "description": "A JSON object containing a property for each extension property. The key is the property name,\nthe value is the property value"
          },
          "createTime": {
            "type": "string",
            "format": "date-time",
            "nullable": true,
            "description": "Time of task creation. \n[Default format](https://docs.camunda.org/manual/7.22/reference/rest/overview/date-format/)\n`yyyy-MM-dd'T'HH:mm:ss.SSSZ`."
          }
        }
      },
      "TaskDto": {
        "properties":{
          "taskState": {
            "type": "string",
            "nullable": true,
            "description": "The task's state."
          }
        }
      }
    }
  }
}
